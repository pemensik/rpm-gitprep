# gitprep specific
# Here comes the hack
%_rpmpatch ~/bin/rpmpatch
%_rpmfixperms ~/bin/rpmfixperms
# original_value from lib 
# is there way to use old %_fixperms value without recursion?
%_orig_fixperms %{__chmod} -Rf a+rX,u+w,g-w,o-w
# Disable these two to disable gitprep
#  %_fixperms %{_rpmfixperms} %{_orig_fixperms} --git=%{__git} --
#  %__patch %{_rpmpatch} --git=%{__git}
